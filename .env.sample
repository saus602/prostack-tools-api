# ProStack Tools API — environment
APP_NAME=ProStack Tools API
APP_ENV=local
APP_DEBUG=true

# WooCommerce webhook/bridge secret (used to validate X-Woo-Signature)
WOO_WEBHOOK_SECRET=change_me_long_random_string

# If you add Postgres later, put DSN here
DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/prostack

# CORS (comma-separated origins). Example: https://yourwebsite.com
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Mock mode flag (true keeps predictable demo data)
MOCK_MODE=true
